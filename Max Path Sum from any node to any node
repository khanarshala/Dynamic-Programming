int Solve(Node*root, int &, res);
{
if(root==nullptr)
return 0;
int l=Solve(root->left,res);
int r=Solve(root->right,res);
int temp=max(max(l,r)+root->value, root->val);
int ans= max(temp,l+r_root->val;
res= max(res,ans);
return temp;
}
